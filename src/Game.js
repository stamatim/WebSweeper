import React from 'react';
import ReactDOM from 'react-dom';

/*
* The component for the game
*
* Stores the height and width of the board, which is based on the difficulty selected in the menu
* Stores the number of mines
*/
let boardHeight; // The height of the board
let boardWidth; // The width of the board
let numMines; // The number of mines on the board

class Game extends React.Component {
    
}